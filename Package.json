{
  "name": "GFX.GPU",
  "version": "0.2.0",
  "description": "Direct GPU resources, pipelines, commands, compute, rendering, and presentation.",
  "authors": ["Matanek"],
  "requires": { "silex": ">=0.39.0" },
  "dependencies": {
    "GFX": "^0.37.0"
  },
  "boundary": {
    "macos-arm64": {
      "providers": {
        "SDL3": {
          "requires": ["GFX.SDL3"]
        }
      }
    },
    "linux-x64": {
      "providers": {
        "SDL3": {
          "requires": ["GFX.SDL3"]
        }
      }
    },
    "windows-x64": {
      "providers": {
        "SDL3": {
          "requires": ["GFX.SDL3"]
        }
      }
    },
    "windows-arm64": {
      "providers": {
        "SDL3": {
          "requires": ["GFX.SDL3"]
        }
      }
    }
  }
}
